const fs=require('fs'),b=fs.readFileSync('LMFeditor.exe');let pe=b.readUInt32LE(60),sp=b.readUInt32LE(pe+12),n=b.readUInt32LE(pe+16),st=sp+n*18;let res=JSON.parse(fs.readFileSync('analysis/resources.json'));console.log('sym',sp,n);for(let i=0;i<n;i++){let o=sp+i*18,name=b.readUInt32LE(o)===0?b.toString('ascii',st+b.readUInt32LE(o+4)).split('\0')[0]:b.toString('ascii',o,o+8).split('\0')[0];let v=b.readUInt32LE(o+8),sec=b.readInt16LE(o+12);if(/TileBitmapLoad|LoadMapFile|Mapinfo/.test(name)){console.log(name,v,sec);if(sec>0){let s=res.ss[sec-1],pos=s.off+v;fs.writeFileSync('analysis/'+name.replace(/[^\w]/g,'')+'.bin',b.subarray(pos,pos+5000));console.log(b.subarray(pos,pos+180).toString('hex'));}}i+=b[o+17];}
