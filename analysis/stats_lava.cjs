const fs=require('fs');const d=fs.readFileSync('영상/용암.LMF');let a=new Map();for(let o=32;o<d.length;o+=8){let id=d.readUInt32LE(o),x=d.readInt16LE(o+4),y=d.readInt16LE(o+6);a.set(x+','+y,id)};for(const id of [7,49,86,100,110,111,112,113,114,115,116,117]){let p=[];for(const kv of a){if(kv[1]===id){let q=kv[0].split(',').map(Number);p.push(q)}}if(p.length){let xs=p.map(q=>q[0]),ys=p.map(q=>q[1]);console.log(id,Math.min(...xs),Math.min(...ys),Math.max(...xs),Math.max(...ys),p.length)}}
