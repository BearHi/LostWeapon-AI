const fs=require('fs');const p='지옥_새설계1.LMF',d=fs.readFileSync(p);let rec=[];for(let o=32;o<d.length;o+=8){let id=d.readUInt32LE(o);if(id>=101&&id<=109)continue;rec.push({id,x:d.readInt16LE(o+4),y:d.readInt16LE(o+6)})}let out=Buffer.from(d.subarray(0,32));out.writeUInt32LE(rec.length,21);for(const b of rec){let q=Buffer.alloc(8);q.writeUInt32LE(b.id);q.writeInt16LE(b.x,4);q.writeInt16LE(b.y,6);out=Buffer.concat([out,q])}fs.writeFileSync(p,out);console.log(rec.length)
