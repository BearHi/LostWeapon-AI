# LostWeapon AI: 프로젝트 마스터 운영 지침서 (Master AI Instructions)

> **새 세션(새 채팅) 시작 시 AI 필독 사항**:  
> 본 프로젝트에 투입되는 모든 AI는 작업을 시작하기 전 반드시 이 문서를 정독하고, 여기에 정의된 규칙과 기검증된 사실을 기반으로 작업해야 합니다. 임의로 아키텍처를 갈아엎거나 기검증된 사실을 재검증하느라 토큰을 낭비하지 마십시오.

---

## 1. 프로젝트 개요 및 최종 목표
* **목표**: 고전 런앤건 액션 게임 *로스트웨폰(LostWeapon)*에서 유즈맵 자율주행(깃발 도달) 및 칼전(PvP)이 가능한 자율 학습형 AI 구축.
* **GitHub 저장소**: [https://github.com/BearHi/LostWeapon-AI](https://github.com/BearHi/LostWeapon-AI)
* **로컬 절대 경로**: `C:\Users\sang\Desktop\로스트웨폰 맵`

---

## 2. 개발 환경 및 도구 경로
* **Python 실행 파일**: `C:\Users\sang\AppData\Local\Programs\Python\Python311\python.exe`
* **Git 실행 파일**: `C:\Users\sang\AppData\Local\Programs\Git\cmd\git.exe`
* **GPU**: NVIDIA GeForce GTX 1060 6GB (Pascal sm_61, PyTorch 2.5.1+cu121 CUDA 가속 활성화됨)
* **물리 시뮬레이터**: 로컬 x86 에뮬레이터(`native_harness/api.py`의 `SimpleTrainingAPI`)를 통해 파이썬 내부에서 초당 1,000틱 이상 무인 고속 실행 가능 (클라이언트 GUI 창 불필요).

---

## 3. 기검증된 핵심 물리 및 시스템 팩트 (절대 번복/재검증 금지)

1. **데이터 Split & 누설 실측 (75,333건 전수 조사 완료)**:
   * Validation 세트의 **98.43%가 Train 세트와 동일한 탐색 트리 궤적(Direct Lineage) 상에 위치**함 (궤적 누설 확인).
   * **Legacy 데이터**는 미지의 Heldout 맵에서도 BSS +0.1531로 상수를 이기지만, **Planned 데이터**는 Heldout에서 BSS -0.3663으로 폭락하여 전체 15회 승격 실패를 이끌었음.
   * Heldout 맵(훈13, 훈19, 훈3)은 위험 사건 발생률이 0~4%인 극단적 저빈도 맵임.
2. **맵 바이너리 규격 (`.LMF`)**:
   * 32바이트 바이너리 헤더 (`NewLWMapFile_1.0`) + 8바이트 레코드 구조 (`uint32 tile_id`, `int16 x`, `int16 y`).
   * 1개 타일 셀 = $32 \times 32$ 픽셀.
   * **ID 100**: 플레이어 스폰 위치 (시작점)
   * **ID 140**: 깃발 / 베이스 (골인 지점, raw140)
   * **ID 7, 8**: 일반 사각 발판 (Square Solid)
   * **ID 49**: 용암 / 낙사 리셋 킬존 (Instant Reset)
   * **ID 4**: 스프링 (Spring, 수직 고도 도약)
   * **ID 19, 27, 37**: 얼음 (Ice, 마찰력 0 관성 미끄러짐)
   * **ID 52~55**: 자석 4방향 (Magnet)
3. **플레이어 물리 메모리 좌표 기준**:
   * 평지 발판(y=14) 위를 정상 보행할 때의 캐릭터 실제 Y좌표는 **`448.0`**임.
   * **[주의]** 절대 `Y > 430` 같은 수치로 낙사 판정을 내리지 말 것! 진짜 낙사는 발판 하단으로 추락한 **`Y > 470.0`**임.
4. **실제 점프 클리어 성공 이력**:
   * 3칸 낭떠러지 점프맵(`훈2.LMF`)에서 73틱째에 낭떠러지 돌파(착지 X=386.0), 169틱째에 깃발(X=784) 도달 클리어 실측 성공함 (`TRAINING_LOG.md` 6회차 기록).

---

## 4. AI 작업 및 태도 수칙 (Rules of Engagement)

1. **아부 및 과장 절대 금지**:
   * "완벽합니다", "최고의 지적입니다" 같은 영혼 없는 아부성 멘트 일절 금지.
   * 사용자의 말이 항상 맞는 것은 아니므로, 감정적 동조 대신 오직 **데이터와 코드 사실(Fact)**로만 확인하고 보고할 것.
2. **무한 루프 절대 금지**:
   * "성공할 때까지 돌린다"는 식의 무한 루프 코드는 절대 작성 금지.
   * 모든 테스트와 훈련은 **최대 5~10회 시도 제한**을 걸고, 2~5초 내에 결과를 콘솔과 로그에 보고하고 종료할 것.
3. **로컬 연산 우선 (토큰 절약)**:
   * 복잡한 연산, 맵 생성, 시뮬레이션은 사용자 PC 로컬 파이썬과 GPU에서 돌리고, 대화창에는 3~5줄 이내의 핵심 요약 결과만 출력할 것.
4. **모든 실패와 결함 투명 기록**:
   * 버그나 실패가 발생하면 숨기거나 어물쩍 넘어가지 말고, `EXPERIMENT_BACKLOG.md`의 `Incident & Failure Log`에 일시, 원인, 수정 조치를 투명하게 기록할 것.
5. **Git 커밋 필수**:
   * 코드를 수정하거나 유의미한 진전이 있을 때마다 명확한 커밋 메시지와 함께 커밋하고 GitHub에 푸시할 것.
6. **특수문자 및 한글 인코딩 깨짐 원천 방지**:
   * Windows 배치 파일(`.bat`) 및 콘솔 출력 스크립트는 항상 `@chcp 65001 > nul`(UTF-8)을 적용하여 한글 깨짐을 원천 방지할 것.
   * IDE/에디터에서 열었을 때 외계어/바이너리 특수문자(`LF@ |範...`)를 유발하는 `.lnk` 바로가기 대신, 표준 UTF-8 `.bat` 실행 파일을 단일 진입점으로 유지할 것.

---

## 5. 핵심 파일 구조 및 역할

* `EXPERIMENT_BACKLOG.md`: 전체 마일스톤, 실험 대기열(Exp 1A~Exp 3B), 버그/사고 기록 대시보드
* `TRAINING_LOG.md`: AI가 실제로 맵을 돌며 깃발을 딴 실시간 에피소드 성장 일지
* `PROJECT_INSTRUCTIONS.md`: 본 마스터 지침서 (새 세션 인수인계용)
* `autonomous_curriculum_trainer.py`: 난이도별 맵 자동 생성 및 고속 훈련 러너
* `native_harness/checkpoints/baseline_snapshot_20260922/`: 원본 코드 및 가중치 영구 동결 백업
* `훈련용맵/`: 로스트웨폰 원본 훈련 LMF 맵들 및 자동 생성 커리큘럼 맵

---

## 6. 상태별 키 입력 검증 및 물리 한계 필수 지침 (State-Action & Physics Invariants)

모든 AI 에이전트, 경로 탐색기(Planner), 훈련 스크립트 및 맵 생성기는 [STATE_ACTION_INPUT_MATRIX.md](file:///c:/Users/sang/Desktop/로스트웨폰%20맵/STATE_ACTION_INPUT_MATRIX.md)와 [PHYSICS_DOMAIN_RULES.md](file:///c:/Users/sang/Desktop/로스트웨폰%20맵/PHYSICS_DOMAIN_RULES.md)의 물리 규격을 **$100\%$ 통과해야만 하며, 하나라도 위배될 경우 즉시 검증 실패(FAIL) 처리**한다.

1. **앉기 상태 (`state38=6, c0=1`)**:
   * 앉은 상태에서 `Z` 입력 시 별도 특수 모션 없이 **그대로 지상 기본 칼질이 즉시 발동**됨.
2. **구르기 상태 (`c0=4` 앞굴, `c0=3` 뒷굴)**:
   * **`UP` 절대 불가**: 구르기 도중 점프 캔슬은 불가능하며 키가 완전히 무시됨.
   * **`LEFT/RIGHT` 절대 불가**: 구르기 도중 좌우 방향키는 궤적에 영향을 주지 못하고 완전히 무시됨 (관성 궤적 고정).
   * **낙하산(`C`) 타이밍 제한**: 구르기 초반에는 `C`가 잠겨 작동하지 않으며, 구르기 후반/정점 이탈 특정 타이밍에만 낙하산 전개가 허용됨.
3. **공중 2단 점프 절대 불가**:
   * 캐시 아이템 여부와 무관하게 시스템상 공중에서 `UP`은 $100\%$ 불가능으로 간주하며 입력 시도 시 즉시 기각.
4. **낙하산 상태 (`dc=1, dc=2`)**:
   * `Z`(공격), `UP`, `DOWN` 입력 절대 금지 (엔진에서 무시됨).
   * `C`(접기)는 환풍기나 상향 자석에 의해 강제 상승할 때 **상승을 캔슬하고 바닥으로 떨어지기 위한 용도**로 사용.
5. **얼음 지형 판정**:
   * 평지 얼음 위에 발이 닿아 서 있는 상태에서는 지상 판정이므로 **낙하산(`C`) 불가**.
   * 얼음 슬로프를 타고 미끄러지다가 낭떠러지 허공으로 떨어지는 순간부터만 **공중 낙하산(`C`) 허용**.
6. **사다리 상태 (`state38=0, state74=1`)**:
   * `UP`, `DOWN`은 오직 수직 승강($4.0\text{px/tick}$)으로만 동작함.
   * `Z`(공격), `C`(낙하산), `SPACE`, 수평 단독(`LEFT`, `RIGHT`)은 엔진 레벨에서 무시되므로 액션 생성 금지.
   * 사다리 이탈은 반드시 상향 대각선(`LEFT+UP`, `RIGHT+UP`)으로만 수행할 것.
7. **1칸 고립 기둥 착지 (폭 $32\text{px}$)**:
   * 1번칼(단검) 사용 절대 금지 ($\Delta X = +119\text{px}$ 강제 돌진으로 100% 추락).
   * 좁은 기둥 안착은 반드시 4번칼(또는 2, 3번 제자리칼, $\Delta X = 0.0\text{px}$) 1회 탭으로만 수행할 것.
8. **수중 `X` 폭탄**:
   * 총전/폭탄전 배제 방침에 따라 완전 미사용 처리.
9. **물리적 절대 도달 한계선 준수**:
   * 순수 점프 최고 고도: $186.0\text{px}$ ($5.81$칸)
   * 점프 뒷굴 최고 고도: $270.0\text{px}$ ($8.44$칸) $\implies 9$칸($288\text{px}$) 이상은 스프링 없이 절대 도달 불가.
   * 지상 단일 도약 최장 비거리: 지연 뒷굴 $506.0\text{px}$ ($15.81$칸) $\implies 18$칸 이상은 낙하산이나 징검다리 없이 절대 직행 불가.
10. **천장(오버행) 충돌 물리 반응**:
    * 머리가 천장 하단에 닿는 즉시 상승 속도 `motion58`은 $0.0$으로 소멸되며, 구르기 모션(`st38=2`)은 캔슬되지 않고 유지된 채 중력($g=-40$)에 의해 아래로 낙하함.
11. **사라지는 블록(raw124 / ID 36) 점프 윈도우**:
    * 밟은 순간부터 **정확히 12틱 ($192\text{ms}$)** 동안만 지상 충돌 판정이 유지되며, 12틱 이내에 `UP`을 눌러야만 도약 가능. (13틱째 충돌 소멸, 15틱째 추락, 143틱 완벽 주기).
12. **경사로(Slope) 오르막 보행**:
    * 오르막 등판 시 수평 이동 감속 페널티가 일절 없으며 평지와 동일한 $v_x = 4.0\text{px/tick}$ 유지.
13. **검증 통과 기준 (Pass/Fail Criteria)**:
    * 위 규칙을 위배하는 조작을 시도하는 에이전트는 액션 필터에서 즉시 마스킹(Masking) 및 감점 처리.
    * 위 한계선을 고려하지 않아 날먹이 발생하는 맵은 검증 파이프라인에서 자동 반려(Reject).

---

## 7. 새 세션에서 바로 이어받아 해야 할 작업 (Current Next Task)

1. **유저의 상태별 키 입력 매트릭스([STATE_ACTION_INPUT_MATRIX.md](file:///c:/Users/sang/Desktop/로스트웨폰%20맵/STATE_ACTION_INPUT_MATRIX.md)) 검토 피드백 반영**:
   * 유저 검토 후 수정/보완 사항 즉시 반영.
2. **11종 벤치마크 로컬 원클릭 실행 상태 유지**:
   * [11종_전수검증_원클릭.bat](file:///c:/Users/sang/Desktop/로스트웨폰%20맵/11종_전수검증_원클릭.bat) 기반으로 사용자 로컬 무인 실행 및 결과 수신 체계 유지.
3. **Git 커밋 및 푸시**:
   * `PHYSICS_DOMAIN_RULES.md`, `STATE_ACTION_INPUT_MATRIX.md`, `PROJECT_INSTRUCTIONS.md` 변경사항 동기화.
