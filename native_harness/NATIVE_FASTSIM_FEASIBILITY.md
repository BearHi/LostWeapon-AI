
## 훈7 ladder map (2026-09-13)

- Full normal Client capture: `private_snapshots/hun7_live.zip` (26x22, 61 records, start `(80,576)`).
- Native `0x41e510` materialization produced all 61 runtime objects exactly.
- Initial direct materializer probe left 10 ladder cells absent from grid-1. Comparing the original capture showed those exact cells use climb mask code `3`; this is a map-loader post-pass, not a physics rewrite. `NativeLmfOracle` now applies that native loader pass and all four grids match the capture exactly.
- Oracle schedule `RIGHT 64 ticks` to `(336,576)`, then `UP` advances y by 4 pixels per tick with ladder state (`0x38=0`, `0x74=1`).
- A first normal input attempt at the live client did not climb, because the client had already remained at a post-trace position and was not reset to the captured start. It is not counted as parity evidence. A fresh same-boundary normal trace is still required before marking ladder parity confirmed.
- Fresh ladder-start capture at `(336,576)` followed by ordinary `UP` input reached `(336,348)` in the normal Client. The observed path is a strict 4-pixel-per-tick ladder path; offline native oracle produced the same path/rate (normal sampler omitted two intermediate boundaries, so this is movement-path parity, not a claim of every sampled boundary).
- Evidence: `evidence/normal_hun7_ladder_up_trace.json`, `evidence/hun7_ladder_oracle_parity.json`.

## 훈8.5 exploratory probe

- The 56-record map loads through the native builder and passes one world tick.
- Offline probes for `RIGHT`, `RIGHT+UP` timed near the ID4 object, and longer jump-over sequences were recorded in `evidence/hun85_oracle_exploratory_probe.json`.
- These probes did not yet establish a spring impulse. This is deliberately left unconfirmed until a normal-Client trace identifies the correct contact/input boundary; no spring parity claim is made.

## 훈8.5 live capture / builder comparison (2026-09-14)

- A read-only live capture was taken while 훈8.5 was loaded: `private_snapshots/hun85_live.zip` (56 runtime objects; player at `(263,576)`).
- Rebuilding `훈8.5.LMF` with the original materializer `0x41e510` produced the same 56 object records byte-for-byte, including the runtime-adjusted spring object position. This is loader/object-state evidence, not a gameplay-tick parity result. Evidence: `evidence/hun85_live_native_builder_object_parity.json`.
- The attempted normal-Client spring input trace did not produce a usable trace because the Client exited before input could be delivered. The vertical spring therefore remains `HYPOTHESIS` pending a fresh same-boundary normal-Client trace.

- A later read-only `LEFT+UP` boundary trace did survive. Its sampled jump path corresponded to the captured original-x86 replay at the same positions/heights (including `(316,394)`, `(280,426.4)`, and `(248,482.4)`); the samples are asynchronous and are recorded as trajectory correspondence, not a full tick-by-tick spring parity result. Evidence: `evidence/hun85_jump_trajectory_correspondence.json`.
- A bounded native schedule search found a reproducible vertical-spring candidate: start grounded near `x=136..152`, press `RIGHT+UP` for the jump edge, then keep `RIGHT`; the original x86 closure reaches `y=547` over the spring and writes `motion58=+2000` around tick 58. This is the next normal-Client action card, not yet a parity confirmation. Evidence: `evidence/hun85_spring_schedule_search.json`.
- The first normal-Client microtest using that schedule succeeded in triggering the spring. Comparing the same grounded boundary and selected gameplay fields against the native replay matched 55/58 unique normal samples, including the post-trigger native tick-59 state `(x=380,y=527,motion58=1960)`; the normal sampler skipped the immediate tick-58 `(motion58=+2000)` boundary. Vertical spring is now `NATIVE_PARITY_CONFIRMED` for this fixture and input window. Evidence: `evidence/hun85_user_microtest_normal.json`, `evidence/hun85_spring_normal_native_parity.json`.

## NativeTrainingAPI regression (2026-09-13)

`NativeTrainingAPI` now passes load, save/restore, and 8-tick RIGHT replay for all 23 supplied training LMFs using the captured Client closure. Evidence: `evidence/native_training_api_all_maps.json`. This is API determinism evidence only; it does not replace normal-Client parity fixtures.

## Dirty branch snapshots (2026-09-14)

`NativeTrainingAPI.save_state()` / `restore_state()` now use a process-local dirty-page branch snapshot after the one full baseline capture. On 훈2 using the 훈1 runtime base, three repetitions saved 41 pages / 167,936 bytes, restored in a median 3.54 ms, and replayed the same state exactly. The full captured runtime base is 550,215,680 bytes; this benchmark does not claim that every map has the same 41-page footprint. Evidence: `evidence/branch_snapshot_benchmark.json`.

The same branch API now passes 훈2, 훈5, 훈6, 훈7, and 훈12. Dirty footprints were 41, 41, 41, 42, and 51 pages respectively; every save/advance/restore comparison was exact. Evidence: `evidence/branch_snapshot_sweep.json`.

## 훈12 water-state probe

- Current live capture is a stable water state at `(80,304)`, `state38=21`, `stateC0=21`.
- Rebuilding 훈12 through the native builder and stepping 30 NOOP ticks from that stable state preserves `(80,304)` and water state, so the post-entry closure is deterministic.
- The initial spawn-to-water fall boundary remains unverified because the live capture occurred after the automatic fall; evidence: `evidence/hun12_stable_water_probe.json`.
- 훈12 current water-state footprint measured at 191 pages (175 Client-image, 10 mapped-private, plus harness pages) across the tested closure. Evidence: `evidence/hun12_world_footprint.json`.
- 훈12 current capture comparison: all 150 ID123 water objects have four runtime-field differences from freshly materialized records (offsets `0x08`, `0x4c`, `0x54`). This is a coherent water-object update layer, not random memory noise; it explains why a fresh native build enters water before reproducing the normal fall. The start-fall parity task therefore requires the water-object initialization/update closure, not a player gravity patch.
- Follow-up: stepping the native world chain does update ID123 water fields (`0x54` advances each tick, `0x4c` later toggles), so the water update function is present in the current closure. The remaining mismatch is specifically the initial player/water contact ordering and vertical displacement before the stable `(80,304)` state; do not treat the earlier “missing update layer” wording as final.
- A controlled native run initialized at the observed post-fall coordinate `(80,304)` reproduces the live water transition exactly: first tick sets `C0=21` while y remains 304, then `state38=21`. This confirms the stable post-fall closure; only the pre-capture spawn coordinate/timing remains uncertain.
- A read-only 1-second sample of the currently running normal Client on 훈12 found 49 observations with identical gameplay-core fields `(x,y,motion58,state38,state68,state74,stateB0,C0,C4,D0,DC)`; only animation frame `0x3c` advanced. This independently confirms that the captured post-entry water state is stable in the normal Client. Evidence: `evidence/normal_hun12_stable_water_trace.json`. It does not close the spawn-to-water fall boundary.
- A controlled native sensitivity sweep found that a grounded player initialized at `(80,192)` follows the original x86 water path to `(80,304)` by tick 60, while the generic LMF spawn placement `(80,224)` contacts water immediately. This isolates the remaining issue to spawn initialization/contact ordering; it is recorded as an exploratory candidate, not a hardcoded coordinate or parity claim. Evidence: `evidence/hun12_spawn_y_sweep.json`, `evidence/hun12_spawn_y192_long.json`.
- Instrumenting that controlled run shows the transition is produced by original Client code: at tick 15 the falling state keeps `state38=9` but writes `C0=21` and continues from `y=223.2` to `228.0`; tick 16 writes `state38=21` and continues the water movement. This is a trace of the existing closure, not a Python water rule. Evidence: `evidence/hun12_water_transition_write_trace.json`.

## All-fixture native behavior sweep (2026-09-13)

All 23 LMF fixtures were loaded through the native builder and exercised with 30 NOOP, 60 RIGHT, and 60 RIGHT+UP steps. Evidence: `evidence/native_fixture_behavior_sweep.json`. This is exploratory behavior coverage, not normal-Client parity. 훈20 RIGHT+UP hit an emulation fault and is now an explicit follow-up item; the other maps completed the sweep. 훈12 remains the known pre-entry water-boundary exception.
- 훈20 `RIGHT+UP` fault is snapshot-resource dependent: it fails at tick 56 with the 훈1/훈7 captures but runs 70 ticks successfully using `hun12_live.zip` as the captured runtime resource base. Evidence: `evidence/hun20_resource_snapshot_sweep.json`. This is not a map-builder failure; a map-specific resource/state base is required for that fixture.
- Resource-base comparison for 훈18~20 is in `evidence/late_fixture_resource_comparison.json`: 훈18 and 훈19 run 70 ticks on all three tested captures; 훈20 requires the 훈12 live resource base (훈1/훈7 fail at tick 56, 훈12 passes 70 ticks).

## Full resource-base compatibility sweep (2026-09-13)

All 23 fixtures were tested against three captured runtime bases (`hun1_full`, `hun7_live`, `hun12_live`) with 30 NOOP ticks and 30 RIGHT+UP ticks per combination: 23 × 3 × 2 = 138 runs, all PASS. Evidence: `evidence/all_map_resource_compatibility_sweep.json`. The longer 훈20 70-tick test still shows a late tick-56 fault for hun1/hun7 and passes with hun12; short-load compatibility is therefore broad, while long-horizon resource dependence remains a 훈20 follow-up.

## Evidence ledger and handoff integration

The additional handoff is now treated as a verification ledger, not as code truth. Its expected values (gravity, spring launch, magnet force, bomb thresholds, protection behavior, stored ice momentum, roll+C boundaries, and unusual held-input cases) remain regression targets until normal Client traces close them. The current machine-readable ledger is `evidence/mechanic_evidence_ledger.json`.

`BUILD_PASS`, `ONE_TICK_EXECUTION_PASS`, `DETERMINISTIC_REPLAY_PASS`, and `NORMAL_CLIENT_PARITY_PASS` are intentionally separate claims. A broad fixture sweep or API replay pass never upgrades an untested mechanic to normal-Client parity.
- Team-base reconnaissance: raw122 materializes into a separate type700 special-object array rather than the ordinary runtime-object list. In `문어/칼3.lmf`, the two records produce exact base bounds `(209,63,334,194)` and `(1105,63,1230,194)` with opposing `+0x94` team flags `0/1`. Holding newly exposed original `SPACE` input for 60 ticks next to either base does not change it under the current single-player 훈1 capture, so team/session state or an additional update boundary is required. No synthetic installation timer was added. Evidence: `evidence/raw122_builder_write_trace.json`, `evidence/raw122_base_space_offline_probe.json`.
- Two-computer P2P combat is now captured. The read-only trace contains both player slots, attacks 15~18, hit/knockdown states, and coherent HP losses. Swapping roster 0/1 around the unchanged input function lets `TwoPlayerCombatOracle` apply independent policy inputs before one shared original world update; both slots independently enter attack state in regression. A controlled branch placed weapon-1 attacker/target at `(700,736)` / `(760,736)` and reproduced 7 damage (`58→51`, target state38=5) in original x86. The replayed anti-idle explosion is a real rule: about 15 seconds without dealing or receiving a hit causes explosion/knockdown and can destroy raw90 ice. It must be retained while its captured mid-timer phase is reset at episode start; exact reset and raw90-destruction boundaries remain pending normal-Client parity. Evidence: `evidence/team_combat_p2p_trace.jsonl`, `evidence/two_player_input_regression.json`, `evidence/p2p_weapon1_controlled_hit.json`.
- Added `run_training_episode.py` as a reproducible JSON-schedule runner. Smoke artifacts for the currently confirmed fixtures are in `evidence/runner_훈2.json`, `runner_훈5.json`, `runner_훈6.json`, `runner_훈7.json`, and `runner_훈12.json`.
- Native branch sweep for backroll+C timing (훈1 snapshot, C offsets 0..12 after 8 backroll ticks) found a transition only at offset 12: `C0` drops to 0 and `DC` becomes 1 while descending. Earlier offsets stay in backroll. Evidence: `evidence/roll_c_timing_branch_sweep.json`. This is a native-oracle discovery result; normal-Client parity confirmation is still required before promoting it.
- Added `evidence/parity_evidence_index.json` to index every parity artifact and preserve each artifact's scope. Current normal-Client parity artifacts cover 훈2 movement/gap/landing, 훈5 parachute, 훈6 jump path, and 훈7 ladder movement; other fixture sweeps remain exploratory.
- Native contact sweep for 훈8.5 ID4 at x=368 found a vertical launch of `motion58=+2000` when the player contacts the spring around y=536..568. This matches the handoff's expected regression value, but remains `HYPOTHESIS` until a same-boundary normal-Client trace confirms contact timing and fields. Evidence: `evidence/hun85_vertical_spring_contact_sweep.json`.
- 맵테스트4의 raw63 동전블록 아래 `(718,8096)`에서 기록한 첫 점프-뒷굴은 실제 Client와 native replay 모두 `x=748, y=8000`에서 막혔다. 이것은 실패 시도 하나의 일치이며 통과 불가능하다는 결론이 아니다. 이후 사용자가 만든 `동전확인.LMF`에서 실제 Client의 성공 동작은 오른쪽 `(592,608)`에서 왼쪽 뒷굴로 동전줄을 넘어 `(388,416)`에 착지했다. native timing sweep도 빠른 입력에서 정확히 `(388,416)`에 착지하고 늦은 입력에서는 실패해 성공/실패 경계를 모두 재현했다. 이 fixture는 `CLIENT_OUTCOME_PARITY_CONFIRMED`이다. Evidence: `evidence/maptest4_coin_normal_trace.jsonl`, `evidence/maptest4_coin_jump_backroll_replay.json`, `evidence/maptest4_coin_jump_backroll_parity.json`, `evidence/coin_block_backroll_regression.json`.
- Native diagonal spring sweep: 훈9 raw5 at x=400 launches right with `motion58=+1200`, `C0=4`, state38=2; 훈10 raw6 at x=560 launches left with the same magnitude and state. Contact window begins around y=544..568 in the tested placement. Evidence: `evidence/diagonal_spring_contact_sweep.json`.
- 훈9 right-launch normal parity is now confirmed for the tested route. Starting at `(208,448)`, the Client moved left to `x=176`, faced right for one update, then held `RIGHT+DOWN`. The pre-contact descent matched native ticks and the launch state matched exactly at `(414,549)`, `motion58=1200`, `state38=2`, `C0=4`. 훈10 left-launch remains pending. Evidence: `evidence/hun9_diagonal_spring_normal_microtest.json`, `evidence/hun9_diagonal_spring_normal_native_parity.json`.
- 훈10 live normal Client also triggered the mirrored spring: after positioning right and entering a left front roll, it launched left at `(558,540)` with `motion58=1200`, `state38=2`, `C0=4`. The captured-original-x86 route candidate independently produces the same launch tuple and leftward direction. Because the live positioning delay moved the boundary slightly off the native candidate, this is `CLIENT_LOG_CONFIRMED`; exact same-boundary tick alignment is still separate. Evidence: `evidence/hun10_diagonal_spring_normal_microtest.json`, `evidence/hun10_diagonal_spring_route_candidate.json`.
- A write trace of the native spring transitions shows the impulses are emitted by original x86 state/object code: diagonal contact first writes `C0=5`, then `C0=4`, `state38=2`, and the directional motion; vertical contact writes the `+2000` motion path without adding a Python rule. This remains exploratory until normal-Client boundary parity is captured. Evidence: `evidence/spring_transition_write_trace.json`.
- The initial direct-placement fan/magnet NOOP probes did not isolate a unique force signature and remain exploratory. They are superseded for fan and 훈14 raw54 by the route-based parity fixtures below. Evidence: `magnet_contact_probes.json`.
- 훈14 raw54 horizontal magnet is now confirmed for the tested route. The original handler at `0x452461` adds 40 per contact tick to per-player accumulator `0x25ca528[slot]`, caps it at 3000, and writes direction 5 to `0x25ca078[slot]`. In the normal Client, holding RIGHT to x=288 and releasing reproduced the original-x86 acceleration: 79/106 samples were exact selected-field boundaries, 20 were adjacent torn reads, 7 were intra-tick accumulator/direction write windows, and no coherent mismatch remained. Rebuilding 훈14 through original `0x41e510` then replaying the schedule matched the captured world at all 121 boundaries; its 108 initial runtime object records were byte-identical. Evidence: `evidence/hun14_raw54_read_trace.json`, `evidence/hun14_magnet_normal_microtest.json`, `evidence/hun14_magnet_normal_native_parity.json`, `evidence/hun14_native_builder_magnet_parity.json`.
- 훈15 raw55 confirms the mirrored horizontal force. `LEFT` for 20 ticks and release produced direction 2, +40 contact accumulation, then growing leftward displacement in both the normal Client and the original-x86-built world. Of 113 live samples, 78 were exact boundaries, 31 adjacent torn reads, and 4 intra-tick write windows; coherent mismatch was zero. Evidence: `evidence/hun15_native_magnet_route_candidate.json`, `evidence/hun15_magnet_normal_microtest.json`, `evidence/hun15_magnet_normal_native_parity.json`.
- 훈16 raw53 vertical field is confirmed. At x336 the field sets player `+0xD8=2`; a jump then loses 20 vertical-motion units per tick while in range, versus the ordinary 40. The normal trace had 132/143 exact selected-field boundaries and no coherent mismatch after classifying torn and known intra-tick writes. The original `0x41e510` build matched the captured world for all 199 tested boundaries and 34/34 initial object records byte-for-byte. Evidence: `evidence/hun16_vertical_magnet_normal_microtest.json`, `evidence/hun16_vertical_magnet_normal_native_parity.json`, `evidence/hun16_native_builder_vertical_magnet_parity.json`.
- 훈17 raw52 downward field is confirmed. A `RIGHT+UP`, then `RIGHT` crossing sets player `+0xD8=1`; vertical motion then loses 100 units per tick and follows the same fall/reset path in the normal Client and captured x86. Of 97 in-fixture live samples, 88 were exact selected-field boundaries and no coherent mismatch remained after classifying torn and known intra-tick writes. The original `0x41e510` build matched the captured world for all 154 tested boundaries and 33/33 initial object records byte-for-byte. Evidence: `evidence/hun17_vertical_magnet_normal_microtest.json`, `evidence/hun17_vertical_magnet_normal_native_parity.json`, `evidence/hun17_native_builder_vertical_magnet_parity.json`.
- 훈13 fan+parachute is now confirmed for the tested route. From the normal start `(80,608)`, `RIGHT+UP` followed by `RIGHT+C` entered the fan column; `DC` changed to `2`, vertical motion reversed, and the lift converged to `motion58=395`. The normal trace matched 84/95 unique selected-field samples from the captured original-x86 replay, including the exact lift state `(420,521.1,395)`. A few samples observe mid-update boundaries (for example `DC` changes before the next position write) and are not counted as whole-tick matches. Evidence: `evidence/hun13_fan_parachute_normal_microtest.json`, `evidence/hun13_fan_parachute_normal_native_parity.json`.
- The initial 훈18/19 direct-placement probes in `evidence/ice_contact_probes.json` did not isolate the mechanism and are superseded by route fixtures. 훈18 raw27 and 훈19 raw37 are mirrored triangular ice: contact adds 40 per tick to `0x25ca528[slot]`, records direction 2/5 in `0x25ca078[slot]`, retains motion after input release and leaving the slope, then decays by 10 per tick. 훈18 `LEFT+DOWN` 21 ticks then release produced 94/113 exact normal samples plus 19 adjacent torn reads; 훈19 `RIGHT+DOWN` produced 82/109 exact, 26 adjacent torn reads, and one identified input-update window. Both had coherent mismatch 0. The builder comparison exposed a missing full-loader collision-mask pass: raw27/raw37 require grid-1 codes 6/5 after `0x41e510`. Implementing that pass made all four grids byte-exact. After spawn settling, both built maps match 142 route boundaries and every initial runtime object byte-for-byte (훈18 62/62, 훈19 68/68). Evidence: `evidence/hun18_ice_normal_native_parity.json`, `evidence/hun18_native_builder_ice_parity.json`, `evidence/hun19_ice_normal_native_parity.json`, `evidence/hun19_native_builder_ice_parity.json`.
- 훈20 raw124 is a proximity disappearing block, not evidence for the separate raw87/raw88/raw95/raw96 bomb families in the handoff. Its original handler at `0x44eec0` changes object `+0xc8`: player contact adds 20 per tick, clamps at 255, and clears collision; absence subtracts 2 per tick, clamps at 0, and restores grid code 1. The tested fall, respawn, reappearance, standing, disappearance cycle is 143 ticks. Normal Client sampling covered every cycle boundary: 248/346 samples were exact, 97 were adjacent torn reads, one was the identified respawn write window, and coherent mismatch was zero. After phase alignment, the original-built LMF matched all 143 selected state boundaries, all 32 object records, and all four collision grids byte-for-byte. `NativeTrainingAPI` save/restore replayed the whole cycle exactly using 43 dirty pages; measured runs were 107.8 and 110.5 tick/s with a 1.01 ms restore. Evidence: `evidence/hun20_disappearing_block_normal_native_parity.json`, `evidence/hun20_disappearing_block_write_trace.json`, `evidence/hun20_native_builder_disappearing_block_parity.json`, `evidence/hun20_training_api_cycle.json`.
