# 4시간 학습 후 점검

종료 사유는 `time_budget_complete`, 18번째 수집/학습 사이클이었다.
모델 학습 예제는 54,861개(새 요청 계획이 기록된 예제 24,928개)로 늘었다.
검증 위치 오차 40.83px, 제외 맵 56.16px였지만 평가 데이터가 늘었으므로 과거 수치와 동일 조건 비교는 아니다.

실제 후보 선택에 대한 검증이 없고, 모델 교체 시 기존 모델의 제외 맵 위험 성능을 보호하지 않는 문제가 있었다.
기존 active/latest 모델과 보고서, 변경 전 코드를 `checkpoints/shared_physics_v1/review_backup_20260918_200414`에 보관했다.

고정 비교 집합 500개를 원본 x86으로 이미 실행된 후보에서 구성했다.
같은 맵/입력 prefix/상황/예산 내 후보를 비교하며, 요청 계획을 기록하지 않은 과거 예제는 비교 집합에서 제외했다.
후보별 전진·위험·시간으로 정의한 국소 점수는 전역 경로 품질과 다르다.

|동일 모델의 순위 설정|국소 종합 점수|복귀/이탈 선택률|
|---|---:|---:|
|위험 가중치 3|0.851|3.6%|
|위험 가중치 6|0.856|3.4%|
|후보 무작위 평균|0.351|3.386%|

차이는 작으며 통계적으로 안전성 향상을 입증한 결과가 아니다. 안전 판정은 계속 native 시험에 맡긴다.
신규 모델 교체 조건에는 제외 맵 오차/위험 악화 방지와 고정 후보 선택 성능 악화 방지를 추가했다.
다음 실행은 기존 모델과 경험을 이어 사용하며 4시간 예산으로 동작한다.
